// Provided variables for multiply
const num1 = 2;
const num2 = 31;

// Multiply num1 and num2 to get 62
const multiply = num1 * num2;

// Generate a random integer > 0
const random = Math.floor(Math.random() * 100) + 1;

// Variables for mod
const num3 = 28;
const num4 = 6;

// Find remainder of num3 divided by num4
const mod = num3 % num4;

// Find max value in a set
const max = Math.max(5, 10, 15, 20);
